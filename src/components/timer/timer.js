import React from "react";

import "./timer.css"

const Timer = () => {
    return (
        <div className="timer">
            <span className="timer__text">25:00</span>
        </div>
    );
}

export default Timer;